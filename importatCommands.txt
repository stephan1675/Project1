
shell kommandos:
*****************
curl:
curl (Client URL) ist ein Kommandozeilen-Tool, mit dem du Daten von oder zu URLs übertragen kannst. 
Es unterstützt verschiedene Protokolle wie HTTP, HTTPS, FTP, SFTP und mehr.




c++ code:
**********
//Kommandozeile ausführen
system():
system("ls -l");  // Führt "ls -l" in der Shell aus

std::unique_ptr<FILE, decltype(&pclose)> pipe(popen("ls -l", "r"), pclose); //führt shellkommando aus und liest es

 // 1 Stunde schlafen in einer while schlaufe
    std::this_thread::sleep_for(std::chrono::hours(1));

meta chatbot:
*************
curl -X POST "https://api.together.xyz/v1/chat/completions" \
  -H "Authorization: Bearer $TOGETHER_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "meta-llama/Llama-3.3-70B-Instruct-Turbo-Free",
    "messages": [{"role": "user", "content": "What are some fun things to do in New York?"}]
  }'